#include<stdio.h>

int main(void) {

    //Declare two integers
    //and assign values
    int i1=10, i2=200;

    //Declare two floats
    //and assign values
    float fl1=1.11, fl2=2.2222;

    //Print the values (floats with 4 decimal places)
    printf("Two integers are %d %d and two floats are %.4f %.4f\n", i1,i2,fl1,fl2);

    return 0;
}